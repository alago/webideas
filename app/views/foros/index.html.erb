

<table>
  <tr>
    <th>Titulo</th>
    <th>Creador</th>

  </tr>

<% @foros.each do |foro| %>
  <tr>
    <td><%= link_to foro.titulo, foro %></td>
    <td><%= foro.user.email %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Foro', new_foro_path %>
